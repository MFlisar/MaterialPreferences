plugins {
    id 'com.android.application'
    id 'kotlin-android'
    id 'kotlinx-serialization'
    id 'com.github.dcendents.android-maven'
}

group = 'com.github.MFlisar'

android {

    compileSdkVersion setup.compileSdk

    buildFeatures {
        viewBinding setup.enableViewBinding
    }

    defaultConfig {
        minSdkVersion setup.minSdk
        targetSdkVersion setup.targetSdk
        vectorDrawables.useSupportLibrary = true
    }

    buildTypes {
        release {
            minifyEnabled false
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-rules.pro'
        }
    }

    compileOptions {
        sourceCompatibility JavaVersion.VERSION_1_8
        targetCompatibility JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8.toString()
    }
}

dependencies {

    // ------------------------
    // Kotlin
    // ------------------------

    implementation "org.jetbrains.kotlin:kotlin-stdlib-jdk8:${versions.kotlinVersion}"

    implementation "org.jetbrains.kotlinx:kotlinx-serialization-json:${androidx.kotlinSerialization}"

    // ------------------------
    // AndroidX
    // ------------------------

    implementation "com.google.android.material:material:${androidx.material}"
    implementation "androidx.recyclerview:recyclerview:${androidx.recyclerview}"

    implementation "androidx.core:core-ktx:${androidx.core}"
    implementation "androidx.lifecycle:lifecycle-runtime-ktx:${androidx.lifecycle}"

    // ------------------------
    // Library
    // ------------------------

    implementation project(':materialpreferences-core')
    implementation project(':materialpreferences-datastore')
    implementation project(':materialpreferences-preference-screen')
    implementation project(':materialpreferences-preference-screen-input')
    implementation project(':materialpreferences-preference-screen-choice')
    implementation project(':materialpreferences-preference-screen-color')

    implementation "com.github.MFlisar.Lumberjack:lumberjack-library:${versions.lumberjack}"
}